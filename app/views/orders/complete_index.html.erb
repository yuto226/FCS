<% @order.each do |order|%>
<div id = "orders">
<table class = "table table-bordered">
<tbody>
  <tr>
    <td rowspan="2"><%= f.check_box 'order_status', {}, true, false %></td>
    <td colspan="2">テーブル番号:</td>
  </tr>
  <tr>
    <td>
      商品名：<%= order.product.name%>
    </td>
    <td>
      数量：<%= order.amount%>
    </td>
  </tr>
  <tr>
    <th colspan="3">備考</th>
  </tr>
  <tr>
    <td colspan="3"><%= order.product.option%></td>
</th>
  </tr>
  </tbody>
</table>
</div>
<%end%><%= f.button "チェックしたものを完了する。"%>
<div class = "page_nate_area"><%= paginate @order %></div>
<%end%>
